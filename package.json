{
  "name": "stlgamedevsite",
  "version": "0.1.0",
  "description": "New website for the STL Gamedev Community",
  "main": "index.html",
  "scripts": {
    "scss": "sass assets/scss:.",
    "build": "npm run scss",
    "watch:css": "sass --watch assets/scss:.",
    "watch": "npm run watch:css",
    "script": "npm run build && npm run watch",
    "run": "hugo server -D"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.3",
    "jquery": "^3.7.1",
    "node-fetch": "^3.3.2",
    "sass": "^1.79.4"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-go-template": "^0.0.15",
    "watch": "^0.13.0"
  }
}
